import { BgYellowDirective } from './bg-yellow.directive';

describe('BgYellowDirective', () => {
  it('should create an instance', () => {
    const directive = new BgYellowDirective();
    expect(directive).toBeTruthy();
  });
});
